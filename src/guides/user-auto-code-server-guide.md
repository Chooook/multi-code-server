# Руководство по пользовательским сервисам

## 🚀 Быстрый старт

### Доступ к Code Server:
1. Откройте браузер
2. Перейдите по адресу: `http://сервер:ПОРТ`
   - Порт можно узнать следующей командой: `my-code-server-address`

### Первый вход:
- Используйте пароль из файла `~/.config/code-server/config.yaml`

## 🛠 Управление сервисами

### Основные команды:

```bash
# Показать статус сервиса
code-server-control status

# Запустить сервис
code-server-control start

# Остановить сервис
code-server-control stop

# Перезапустить сервис
code-server-control restart

# Показать логи
code-server-control logs
```

## Смена пароля Code Server

Вы можете сменить пароль в файле конфигурации code-server: `~/.config/code-server/config.yaml`
После смены пароля, сервис необходимо перезапустить для применения изменний

## Просмотр логов

```bash
# Все логи (последние 50 строк)
user-service-logs

# 100 строк
user-service-logs 100

# Поиск в логах
journalctl --user --grep="error"
```

## Пересоздание конфигураций

Если возникли проблемы с сервисами:
Обратитесь к администратору :) пока что так

## SSH доступ и проброс портов

### Подключение к серверу:

```bash
ssh ваш_пользователь@адрес_сервера
```

### Проброс портов на локальную машину:

```bash
# Проброс порта Code Server
ssh -L 8080:localhost:ВАШ_ПОРТ ваш_пользователь@адрес_сервера
```
После этого Code Server будет доступен локально:
- Локально: `http://localhost:8080`
- На сервере: `http://сeрвер:ВАШ_ПОРТ`

### Постоянный проброс (в фоновом режиме):

```bash
# Создать туннель в фоне
ssh -N -L 8080:localhost:ВАШ_ПОРТ ваш_логин@сервер &
# или с авто-переподключением (требует установки доп утилит)
autossh -M 0 -N -L 8080:localhost:ВАШ_ПОРТ ваш_логин@сервер &
```

## Устранение неполадок

### Сервисы не запускаются:

```bash
# Проверить статус
user-service-control status

# Посмотреть ошибки
user-service-logs 100

# Перезагрузить конфигурации
systemctl --user daemon-reload

# Перезапустить сервис
systemctl --user restart code-server
```

### Нет доступа к веб-интерфейсу:

- Проверьте что сервисы запущены: user-service-control status 
- Проверьте что порт правильный (см. выше)
- Проверьте пароль
- Проверьте правильность проброса портов

### Systemd --user недоступен:

```bash
# Проверить linger
loginctl show-user $(whoami) | grep Linger

# Обычное решение - перелогин
logout
# и снова login
```

## Поддержка

Если проблемы не решаются:

- Сообщите администратору ваше имя пользователя
- Предоставьте логи ошибок: user-service-logs 100 
- Укажите шаги для воспроизведения проблемы

## Дополнительные возможности

### Мониторинг активности:

```bash
# Активные соединения
ss -xp | grep code-server

# Процессы code-server
ps aux | grep code-server | grep -v grep
```
